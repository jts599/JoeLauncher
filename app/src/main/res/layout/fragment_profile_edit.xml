<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.profiles.ProfileEditFragment"> <!-- Adjust tools:context -->

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp">

            <!-- Profile Name -->
            <com.google.android.material.textfield.TextInputLayout
                android:id="@+id/textFieldProfileNameLayout"
                style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                android:hint="Profile Name">

                <com.google.android.material.textfield.TextInputEditText
                    android:id="@+id/editTextProfileName"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:inputType="textCapWords"
                    android:maxLines="1" />
            </com.google.android.material.textfield.TextInputLayout>

            <!-- Profile State Toggle (On, Off, Schedule) -->
            <TextView
                android:id="@+id/textViewStateLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                android:text="Profile State:"
                android:textAppearance="?attr/textAppearanceSubtitle1"
                app:layout_constraintTop_toBottomOf="@id/textFieldProfileNameLayout"
                app:layout_constraintStart_toStartOf="parent"/>

            <com.google.android.material.button.MaterialButtonToggleGroup
                android:id="@+id/toggleGroupProfileState"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                app:singleSelection="true"
                app:selectionRequired="true"
                app:layout_constraintTop_toBottomOf="@id/textViewStateLabel"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"> <!-- Center it or align start -->

                <Button
                    android:id="@+id/buttonStateOn"
                    style="?attr/materialButtonOutlinedStyle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="On" />

                <Button
                    android:id="@+id/buttonStateOff"
                    style="?attr/materialButtonOutlinedStyle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Off" />

                <Button
                    android:id="@+id/buttonStateSchedule"
                    style="?attr/materialButtonOutlinedStyle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Schedule" />
            </com.google.android.material.button.MaterialButtonToggleGroup>

            <!-- Select Apps Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/buttonSelectApps"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="Select Allowed Apps"
                app:icon="@drawable/ic_apps" 
                app:layout_constraintTop_toBottomOf="@id/toggleGroupProfileState"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"/>

            <!-- Scheduling Options (Conditional Visibility) -->
            <androidx.constraintlayout.widget.Group
                android:id="@+id/groupScheduleOptions"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="gone" 
                app:constraint_referenced_ids="textViewDaysOfWeekLabel, linearLayoutDaysOfWeek,
                                              textViewStartTimeLabel, buttonStartTime,
                                              textViewEndTimeLabel, buttonEndTime"
                tools:visibility="visible"/> <!-- Visible in preview -->


            <TextView
                android:id="@+id/textViewDaysOfWeekLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                android:text="Repeat on Days:"
                android:textAppearance="?attr/textAppearanceSubtitle1"
                app:layout_constraintTop_toBottomOf="@id/buttonSelectApps"
                app:layout_constraintStart_toStartOf="parent"/>

            <!-- Using a LinearLayout for simplicity for days of week buttons -->
            <LinearLayout
                android:id="@+id/linearLayoutDaysOfWeek"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:orientation="horizontal"
                app:layout_constraintTop_toBottomOf="@id/textViewDaysOfWeekLabel"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent">

                <!-- We will add 7 buttons for days programmatically or define them here -->
                <!-- Example for one button (repeat for others) -->
                <Button
                    android:id="@+id/buttonDayMonday"
                    style="?attr/materialButtonOutlinedStyle"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:minWidth="48dp"
                    android:paddingStart="4dp"
                    android:paddingEnd="4dp"
                    android:text="M" />
                <Button
                    android:id="@+id/buttonDayTuesday"
                    style="?attr/materialButtonOutlinedStyle"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:minWidth="48dp"
                    android:paddingStart="4dp"
                    android:paddingEnd="4dp"
                    android:text="T" />
                <!-- ... Add buttons for W, Th, F, Sa, Su similarly ... -->
                <Button style="?attr/materialButtonOutlinedStyle" android:text="W" android:layout_width="0dp" android:layout_weight="1" android:layout_height="wrap_content" android:minWidth="48dp" android:paddingStart="4dp" android:paddingEnd="4dp" android:id="@+id/buttonDayWednesday"/>
                <Button style="?attr/materialButtonOutlinedStyle" android:text="Th" android:layout_width="0dp" android:layout_weight="1" android:layout_height="wrap_content" android:minWidth="48dp" android:paddingStart="4dp" android:paddingEnd="4dp" android:id="@+id/buttonDayThursday"/>
                <Button style="?attr/materialButtonOutlinedStyle" android:text="F" android:layout_width="0dp" android:layout_weight="1" android:layout_height="wrap_content" android:minWidth="48dp" android:paddingStart="4dp" android:paddingEnd="4dp" android:id="@+id/buttonDayFriday"/>
                <Button style="?attr/materialButtonOutlinedStyle" android:text="Sa" android:layout_width="0dp" android:layout_weight="1" android:layout_height="wrap_content" android:minWidth="48dp" android:paddingStart="4dp" android:paddingEnd="4dp" android:id="@+id/buttonDaySaturday"/>
                <Button style="?attr/materialButtonOutlinedStyle" android:text="Su" android:layout_width="0dp" android:layout_weight="1" android:layout_height="wrap_content" android:minWidth="48dp" android:paddingStart="4dp" android:paddingEnd="4dp" android:id="@+id/buttonDaySunday"/>

            </LinearLayout>

            <TextView
                android:id="@+id/textViewStartTimeLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="Start Time:"
                android:textAppearance="?attr/textAppearanceSubtitle1"
                app:layout_constraintTop_toBottomOf="@id/linearLayoutDaysOfWeek"
                app:layout_constraintStart_toStartOf="parent"/>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/buttonStartTime"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="10:00 AM"
                android:text="Select Start Time"
                app:layout_constraintTop_toBottomOf="@id/textViewStartTimeLabel"
                app:layout_constraintStart_toStartOf="parent"/>

            <TextView
                android:id="@+id/textViewEndTimeLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="End Time:"
                android:textAppearance="?attr/textAppearanceSubtitle1"
                app:layout_constraintTop_toBottomOf="@id/buttonStartTime"
                app:layout_constraintStart_toStartOf="parent"/>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/buttonEndTime"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="08:00 PM"
                android:text="Select End Time"
                app:layout_constraintTop_toBottomOf="@id/textViewEndTimeLabel"
                app:layout_constraintStart_toStartOf="parent"/>


            <!-- Spacer to push FAB down if content is short -->
            <Space
                android:layout_width="match_parent"
                android:layout_height="80dp"
                app:layout_constraintTop_toBottomOf="@id/buttonEndTime"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintVertical_bias="1.0"/>


        </androidx.constraintlayout.widget.ConstraintLayout>
    </ScrollView>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabSaveProfile"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        app:srcCompat="@drawable/ic_save" /> <!-- You'll need a save icon -->

</androidx.coordinatorlayout.widget.CoordinatorLayout>